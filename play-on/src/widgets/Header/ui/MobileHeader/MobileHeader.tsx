import { memo } from "react"
import { RouteConfig } from "shared/types";

interface MobileHeaderProps {
  routes: RouteConfig[];
}

export const MobileHeader = memo(({ routes }: MobileHeaderProps) =>{
    return (
        <></>
    )
})